@model IEnumerable<DevQuizzMVC.DTO.QuizzDTO>

@{
    ViewBag.Title = "QuizzFrontList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<link href="~/Content/PagedList.css" rel="stylesheet" />*@
<h2>Liste des Quizz Front-End</h2>
<hr />
<br />


<div class="container">
    @foreach (var item in Model)
    {

        <div class="col-md-4" style="height:320px; padding:10px; margin:10px; border:solid thin solid">
            <div class="col-md-12">
                <img style="height:150px" src="../../Content/picturesQuizz/@item.Picture" />
            </div>

            <div class="col-md-10">
                @Html.ActionLink("Demarrer", "Demarrer", "Quizz", new { id = item.Id }, new { @class = "btn btn-default" })
            </div>
        </div>


    }
    @*<p>
            @Html.ActionLink("Créer un utilisateur", "Create")
        </p>*@
    <!--<table class="table table-striped table-hover">
        <tr>
            <th>-->
    @*@Html.ActionLink("↓", "Index", new { search = Request.QueryString["search"], i = Request.QueryString["i"], sortBy = "nameDesc" })*@
    <!--@Html.DisplayNameFor(model => model.First().Title)-->
    @*@Html.ActionLink("↑", "Index", new { search = Request.QueryString["search"], i = Request.QueryString["i"], sortBy = "nameAsc" })*@
    <!--</th>-->
    @*<th>
            @Html.DisplayNameFor(model => model.First().)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().isAdmin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().Photo)
        </th>
        <th></th>*@
    <!--</tr>

            @*foreach (var item in Model)*@
            {
    <tr>
        <td>-->
    @*<a href="@Url.Action("DoQuizz", "Quizz")">@Html.DisplayFor(modelItem => item.Title)</a>*@
    @*@Html.ActionLink(@Html.DisplayFor(modelItem => item.Title).ToString(), "DoQuizz","Quizz", new { id = item.Id },null)*@
    <!--@*Html.ActionLink(@Html.DisplayFor(modelItem => item.Title).ToString(), "Demarrer", "Quizz", new { id = item.Id }, null)*@
            </td>
        </tr>}

            </table>-->

</div>

@*<table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => m

    </div>

    @*<table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Title)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CategoryId)
                </th>
                <th></th>
            </tr>

        @foreach (var  in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CategoryId)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.Id })
                </td>
            </tr>
        }

        </table>*@
